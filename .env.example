# ============================================================================
# Canção que Encanta — variáveis de ambiente (CLAUDE.md §13)
# Copie para `.env.local` e preencha. NUNCA commitar segredos (.env* é ignorado).
# ============================================================================

# ---- Supabase ----
NEXT_PUBLIC_SUPABASE_URL=https://ehlpmukjdknnyhkycncb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=            # só no servidor — NUNCA expor no client
SUPABASE_DB_SCHEMA=cancao
SUPABASE_STORAGE_BUCKET_FULL=songs-full
SUPABASE_STORAGE_BUCKET_PREVIEW=songs-preview

# ---- App / multi-tenant ----
DEFAULT_TENANT_ID=11111111-1111-4111-8111-111111111111
APP_BASE_URL=http://localhost:3000

# ---- OpenAI (geração/ajuste de letra + transcrição de áudio) ----
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TRANSCRIBE_MODEL=whisper-1

# ---- Suno (música) ----
SUNO_API_KEY=
SUNO_MODEL=V4_5PLUS
SUNO_CALLBACK_URL=https://exemplo.com/api/webhooks/suno

# ---- AbacatePay (PIX) ----
ABACATEPAY_API_KEY=
ABACATEPAY_WEBHOOK_SECRET=
ABACATEPAY_WEBHOOK_URL=https://exemplo.com/api/webhooks/abacatepay
MUSIC_PRICE_CENTS=3990

# ---- Fila (QStash ou Inngest) ----
QSTASH_TOKEN=

# ---- WhatsApp / n8n ----
N8N_WEBHOOK_URL=
# Número do WhatsApp do negócio (só dígitos, formato internacional) p/ o botão "Receber no WhatsApp"
NEXT_PUBLIC_BUSINESS_WHATSAPP=
